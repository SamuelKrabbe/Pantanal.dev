services:
  - type: web
    name: keycloak
    runtime: docker
    dockerCommand: -v start-dev
    envVars:
    - key: KEYCLOAK_ADMIN
      value: admin
    - key: KEYCLOAK_ADMIN_PASSWORD
      value: A4d!25oS8h1%#!
    dockerCommand: -v start-dev